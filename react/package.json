{
  "name": "@windwithfo/react",
  "version": "1.0.0",
  "description": "A react template project",
  "main": "index.js",
  "author": "dongkunshan <windwithfo@yeah.net>",
  "scripts": {
    "vite-spa": "node build/init.mjs --build=vite --single=true",
    "vite-mpa": "node build/init.mjs --build=vite --single=false",
    "webpack-spa": "node build/init.mjs --build=webpack --single=true",
    "webpack-mpa": "node build/init.mjs --build=webpack --single=false"
  },
  "license": "MIT",  "devDependencies": {
    "chalk": "5.3.0",
    "fs-extra": "11.2.0"
  }
}
