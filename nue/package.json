
{
  "name": "create-nue",
  "type": "module",
  "version": "0.1.3",
  "homepage": "https://github.com/nuejs/create-nue",
  "description": "A recommended way to start a Nue project",

  "repository": {
    "url": "https://github.com/nuejs/create-nue.git",
    "type": "git"
  },
  "scripts": {
    "start": "bun run minify && bun run render && bun run compile && bun run serve",
    "serve": "cd www && bun ../scripts/server.js",
    "compile": "bun ./scripts/compile.js",
    "minify": "bun ./scripts/minify.js",
    "render": "bun ./scripts/render.js"
  },
  "dependencies": {
    "nuejs-core": "0.1.0",
    "js-yaml": "4.1.0"
  }
}